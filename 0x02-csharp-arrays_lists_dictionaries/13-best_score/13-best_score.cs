using System;
using System.Collections.Generic;

class Dictionary
{
    public static string BestScore(Dictionary<string, int> myList)
    {
        var maxValue = dict.Values.Max();
        return mylist.GetByValueKey(maxValue);
    }
}
